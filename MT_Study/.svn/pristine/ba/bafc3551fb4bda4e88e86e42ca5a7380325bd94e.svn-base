package flashCards;

import java.util.ArrayList;


import com.example.mt_study.R;
import android.content.Context;
import android.view.LayoutInflater;
import android.widget.ArrayAdapter;







public class FlashCardSettingsListViewAdapter extends ArrayAdapter<String> {

	private LayoutInflater inflater;
	private Context otherClass;
	private ArrayList<String> settings;
	
	public FlashCardSettingsListViewAdapter(Context context,
			ArrayList<String> data) {
		//need to change once I figure out what I Want them to look ike maybe.
		super(context, R.layout.flash_card_edit_row);
		settings = data;
		otherClass = context;
		inflater = LayoutInflater.from(context);
	}
	
}